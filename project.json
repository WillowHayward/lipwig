{
  "name": "workspace",
  "$schema": "node_modules/nx/schemas/project-schema.json",
  "implicitDependencies": ["js"]
  "targets": {
    "version": {
      "executor": "@jscutlery/semver:version",
      "options": {
        "syncVersions": true,
        "trackDeps": true,
        "postTargets": ["js:deploy", "angular:deploy", "model:deploy", "utils:deploy"]
      }
    },
  }
}
