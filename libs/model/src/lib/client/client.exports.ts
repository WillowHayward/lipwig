// TODO: I'm not sure if it'd be worth exporting the Data interfaces individually. Food for thought.
export { ClientEvent, ClientEventData } from './client.events';
export { ServerClientEvent, ServerClientEventData } from './server.client.events'

export { ClientError } from './client.errors';

export { BaseMessageData as ClientMessageData } from '../common';
